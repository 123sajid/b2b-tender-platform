## Setup
1. Clone the repo
2. Run `npm install` in frontend/ and backend/
3. Run both servers

frontend/
└── src/
    └── pages/
        ├── login.tsx
        ├── register.tsx
        └── dashboard.tsx 